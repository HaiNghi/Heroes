import variable from './../variables/platform';

export default (variables = variable) => {
	const leftTheme = {
		flex: 0.5,
		alignSelf: 'center',
		alignItems: 'flex-start',
	};

	return leftTheme;
};
